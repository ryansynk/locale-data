[project]
name = "locale-data"
version = "0.1.0"
description = "Benchmark and training dataset pipelines for the locale genomic embedding model"
requires-python = ">=3.10"
dependencies = [
    "biopython",
    "jsonargparse",
    "mutation-simulator>=3.0.2",
    "numpy",
    "polars",
    "pysam",
    "tqdm",
    "zstandard",
]
